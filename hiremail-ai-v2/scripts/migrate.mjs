import pg from "pg";import fs from "node:fs/promises";const {Pool}=pg;const sql=await fs.readFile(new URL("../db/schema.sql",import.meta.url),"utf8");const pool=new Pool({connectionString:process.env.DATABASE_URL,ssl:process.env.NODE_ENV==="production"?{rejectUnauthorized:false}:undefined});try{await pool.query(sql);console.log("Database migrated successfully.");}finally{await pool.end();}
