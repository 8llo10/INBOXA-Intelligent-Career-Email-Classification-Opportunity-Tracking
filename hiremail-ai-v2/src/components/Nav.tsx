import Link from "next/link";import { logoutAction } from "@/app/actions";export function Nav({name}:{name:string}){return <nav><div><strong>HireMail AI</strong><span className="muted"> {name}</span></div><div className="navlinks"><Link href="/">الرئيسية</Link><Link href="/settings">الإعدادات</Link><form action={logoutAction}><button className="linkbtn">خروج</button></form></div></nav>}
