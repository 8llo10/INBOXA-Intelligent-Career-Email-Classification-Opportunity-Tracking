import { notFound } from "next/navigation";import { requireUser } from "@/lib/auth";import { getEmail } from "@/services/email.repository";import { FIELD_CATALOG,fieldLabel } from "@/config/fields";import { CATEGORIES } from "@/ai/types";import { feedbackAction } from "@/app/actions";import { Nav } from "@/components/Nav";
export default async function EmailDetails({params}:{params:Promise<{id:string}>}){const u=await requireUser();const {id}=await params;const e=await getEmail(u.id,id);if(!e)notFound();return <><Nav name={u.display_name}/><main className="container narrow"><a href="/">← رجوع</a><section className="card"><h1>{e.subject}</h1><div className="meta"><span>{e.sender_name||e.sender_email}</span><span>{new Date(e.received_at).toLocaleString("ar-SA")}</span></div><div className="analysisgrid"><div><small>التصنيف</small><b>{e.category}</b></div><div><small>المجال</small><b>{fieldLabel(e.detected_field,"ar")}</b></div><div><small>ثقة التصنيف</small><b>{Math.round(e.category_score*100)}%</b></div><div><small>ثقة المجال</small><b>{Math.round(e.field_score*100)}%</b></div><div><small>الشركة</small><b>{e.company||"—"}</b></div><div><small>المسمى</small><b>{e.role_title||"—"}</b></div><div><small>الإجراء</small><b>{e.required_action||"—"}</b></div><div><small>الموعد</small><b>{e.deadline?new Date(e.deadline).toLocaleDateString("ar-SA"):"—"}</b></div></div><h3>الملخص</h3><p className="summary">{e.summary}</p><details><summary>عرض محتوى الرسالة كاملًا</summary><pre className="emailbody">{e.body_text}</pre></details></section><section className="card"><h2>تصحيح الـAI</h2><p>التصحيح يُحفظ كبيانات تدريب خاصة بهذا المستخدم ويؤثر على التحليلات القادمة.</p><form action={feedbackAction} className="feedback"><input type="hidden" name="emailId" value={e.id}/><label>هل هي ذات صلة؟<select name="isRelevant" defaultValue={String(e.is_relevant)}><option value="true">نعم</option><option value="false">لا</option></select></label><label>النوع<select name="category" defaultValue={e.category}>{CATEGORIES.map(c=><option key={c}>{c}</option>)}</select></label><label>المجال<select name="field" defaultValue={e.detected_field}><option value="GENERAL">GENERAL</option>{FIELD_CATALOG.map(f=><option key={f.key} value={f.key}>{f.ar}</option>)}</select></label><button>حفظ التصحيح</button></form></section></main></>}
