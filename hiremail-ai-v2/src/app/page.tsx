import Link from "next/link"; import { requireUser } from "@/lib/auth"; import { listEmails, dashboardStats } from "@/services/email.repository"; import { getGmailConnection } from "@/services/user.repository"; import { syncNowAction } from "./actions"; import { fieldLabel } from "@/config/fields"; import { Nav } from "@/components/Nav";
export default async function Home() { const u = await requireUser(); const [emails, stats, conn] = await Promise.all([listEmails(u.id, { relevant: true, limit: 100 }), dashboardStats(u.id), getGmailConnection(u.id)]); return <><Nav name={u.display_name} /><main className="container"><section className="hero"><div><h1>Professional Opportunity Inbox</h1><p>الذكاء المحلي يقرأ بريدك ويعرض الرسائل المهنية المطابقة لمجالاتك.</p></div>{conn ? <form action={syncNowAction}><button>مزامنة Gmail الآن</button></form> : <Link className="button" href="/settings">اربط Gmail</Link>}</section><section className="stats"><div><b>{stats?.total || 0}</b><span>فرص</span></div><div><b>{stats?.interviews || 0}</b><span>مقابلات</span></div><div><b>{stats?.assessments || 0}</b><span>اختبارات</span></div><div><b>{stats?.offers || 0}</b><span>عروض</span></div><div><b>{stats?.rejections || 0}</b><span>رفض</span></div></section><section className="card"><div className="sectionhead"><h2>الرسائل ذات الصلة</h2><span className="muted">{conn ? `Gmail: ${conn.gmail_email}` : "Gmail غير مربوط"}</span></div><div className="tablewrap"><table><thead><tr><th>المرسل / الشركة</th><th>الملخص</th><th>النوع</th><th>المجال</th><th>التاريخ</th></tr></thead><tbody>{emails.map(e => <tr key={e.id}><td><Link href={`/emails/${e.id}`}><strong>{e.company || e.sender_name || e.sender_email || "—"}</strong><small>{e.subject}</small></Link></td><td>{e.summary || e.snippet}</td><td><span className="badge">{e.category}</span></td><td>{fieldLabel(e.detected_field, "ar")}</td><td>{new Date(e.received_at).toLocaleDateString("ar-SA")}</td></tr>)}{!emails.length && <tr><td colSpan={5} className="empty">ما فيه رسائل مصنفة بعد. اربط Gmail ثم اضغط مزامنة.</td></tr>}</tbody></table></div></section></main></> }
