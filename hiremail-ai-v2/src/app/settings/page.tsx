import { requireUser } from "@/lib/auth";import { getGmailConnection,getUserFields } from "@/services/user.repository";import { FIELD_CATALOG } from "@/config/fields";import { saveFieldsAction, disconnectGmailAction } from "../actions";import { Nav } from "@/components/Nav";
export default async function Settings(){const u=await requireUser();const [conn,selected]=await Promise.all([getGmailConnection(u.id),getUserFields(u.id)]);const all=selected.includes("ALL");return <><Nav name={u.display_name}/><main className="container narrow"><h1>الإعدادات</h1><section className="card"><h2>Gmail</h2>{conn?<><p>متصل مع <strong>{conn.gmail_email}</strong></p><p className="muted">آخر مزامنة: {conn.last_sync_at?new Date(conn.last_sync_at).toLocaleString("ar-SA"):"لم تتم بعد"}</p><div className="inline"><a className="button secondary" href="/api/google/connect">إعادة ربط Gmail</a><form action={disconnectGmailAction}><button className="danger">فصل Gmail</button></form></div></>:<><p>اربط حساب Gmail الذي تريد أن يحلل النظام رسائله.</p><a className="button" href="/api/google/connect">Connect Gmail</a></>}</section><section className="card"><h2>المجالات المستهدفة</h2><p>اختار كل المجالات أو مجموعة محددة. الرسائل المهنية العامة التي لا تذكر مجالًا واضحًا لن تضيع.</p><form action={saveFieldsAction} className="fields"><label className="field all"><input type="checkbox" name="ALL" defaultChecked={all}/> جميع المجالات</label>{FIELD_CATALOG.map(f=><label className="field" key={f.key}><input type="checkbox" name={f.key} defaultChecked={!all&&selected.includes(f.key)}/><span><b>{f.ar}</b><small>{f.en}</small></span></label>)}<button>حفظ المجالات</button></form></section></main></>}
