import { NextResponse } from "next/server";import { requireApiUser } from "@/lib/auth";import { randomToken,sha256 } from "@/lib/crypto";import { query } from "@/lib/db";import { getGoogleAuthUrl } from "@/services/gmail.service";
export async function GET(){try{const u=await requireApiUser();const state=randomToken();await query("INSERT INTO oauth_states(state_hash,user_id,expires_at) VALUES($1,$2,NOW()+INTERVAL '10 minutes')",[sha256(state),u.id]);return NextResponse.redirect(getGoogleAuthUrl(state));}catch{return NextResponse.redirect(new URL("/login",process.env.APP_URL));}}
