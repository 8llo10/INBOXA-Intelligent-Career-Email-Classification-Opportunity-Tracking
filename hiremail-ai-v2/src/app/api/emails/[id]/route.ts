import { NextResponse } from "next/server";import { requireApiUser } from "@/lib/auth";import { getEmail } from "@/services/email.repository";export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){try{const u=await requireApiUser();const {id}=await params;const row=await getEmail(u.id,id);return row?NextResponse.json(row):NextResponse.json({error:"not found"},{status:404});}catch{return NextResponse.json({error:"unauthorized"},{status:401});}}
