import { NextRequest,NextResponse } from "next/server";import { requireApiUser } from "@/lib/auth";import { listEmails } from "@/services/email.repository";export async function GET(req:NextRequest){try{const u=await requireApiUser(),s=req.nextUrl.searchParams;const rows=await listEmails(u.id,{relevant:s.get("relevant")===null?undefined:s.get("relevant")==="true",category:s.get("category")||undefined,field:s.get("field")||undefined,limit:Number(s.get("limit")||100)});return NextResponse.json(rows);}catch{return NextResponse.json({error:"unauthorized"},{status:401});}}
