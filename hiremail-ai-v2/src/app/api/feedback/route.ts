import { NextRequest,NextResponse } from "next/server";import { requireApiUser } from "@/lib/auth";import { addFeedback } from "@/services/email.repository";export async function POST(req:NextRequest){try{const u=await requireApiUser(),b=await req.json();await addFeedback(u.id,String(b.emailId),!!b.isRelevant,String(b.category),String(b.field));return NextResponse.json({ok:true});}catch{return NextResponse.json({error:"invalid"},{status:400});}}
