import { NextRequest,NextResponse } from "next/server";import { requireApiUser } from "@/lib/auth";import { analyzeEmail } from "@/ai/engine";import { getFeedback } from "@/services/email.repository";import { getUserFields } from "@/services/user.repository";export async function POST(req:NextRequest){try{const u=await requireApiUser();const b=await req.json();const a=analyzeEmail({subject:String(b.subject||""),body:String(b.body||""),senderName:b.senderName||null,senderEmail:b.senderEmail||null},await getUserFields(u.id),await getFeedback(u.id));return NextResponse.json(a);}catch{return NextResponse.json({error:"invalid"},{status:400});}}
