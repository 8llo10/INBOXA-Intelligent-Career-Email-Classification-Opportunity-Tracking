import Link from "next/link";import { loginAction } from "../actions";export default async function Login({searchParams}:{searchParams:Promise<Record<string,string>>}){const q=await searchParams;return <main className="auth"><section className="card authcard"><h1>HireMail AI</h1><p>ادخل لحسابك واربط Gmail الخاص بك.</p>{q.error&&<div className="error">البريد أو كلمة المرور غير صحيحة.</div>}<form action={loginAction}><label>البريد<input name="email" type="email" required/></label><label>كلمة المرور<input name="password" type="password" minLength={8} required/></label><button>دخول</button></form><p>ما عندك حساب؟ <Link href="/register">إنشاء حساب</Link></p></section></main>}
